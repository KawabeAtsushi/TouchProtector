<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/background_light</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">#99D1FF</item>
    </style>

    <style name="CircleButton" parent="Widget.Neumorph.ImageButton">
        <item name="neumorph_cornerFamily">oval</item>
        <item name="neumorph_cornerSize">0dp</item>
        <item name="neumorph_inset">10dp</item>
        <item name="neumorph_shadowElevation">5dp</item>
    </style>

</resources>
